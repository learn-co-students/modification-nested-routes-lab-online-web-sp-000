<h1><%= link_to @song.title, song_path(@song) %></h1>
<h3> by <%= display_artist(@song) %> (<%= link_to "Edit Song", edit_artist_song_path(@song.artist, @song) if @song.artist %>) </h3>

<%= link_to "All Songs", songs_path %>
