<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= artist_select(@song,@nested) %>
  <%= f.text_field :artist_name if @nested%>
  <br />

  <%= f.submit %>
<% end %>
