<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :artist_name %>
  <%= artist_select(@song.artist, @song) %>
  <br />

  <%= f.submit %>
<% end %>
