<%= form_for @song  do |f| %>
  <%# artist_id_field(@song) %> 
  <%= f.label "Artist:" %>
  <%= artist_select(@song, @song.artist) %> <br>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <br />

  <%= f.submit %>
<% end %>
