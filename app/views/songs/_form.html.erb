<%= form_for @song  do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :artist_name %>
  <%= display_name(@song, @path) %>
  <%= artist_select(@song, @path) %><br>

  <%= f.submit %>
<% end %>
