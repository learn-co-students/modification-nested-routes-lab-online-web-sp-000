<%= form_for @song do |song| %>
  <%= artist_select(@song.artist, @song) %>
  <%= song.label :title %>
  <%= song.text_field :title %><br>
  <%= song.label :artist_name %>
  <%= song.text_field :artist_name %>
  <br />

  <%= song.submit %>
<% end %>